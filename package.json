{
  "name": "rostanga-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "npm run tauri dev -- --no-dev-server --release --no-watch",
    "start:android": "npm run tauri android dev -- --no-dev-server --release --no-watch",
    "dev": "",
    "build": "tsc && vite build",
    "prepare": "npm run build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@holochain/client": "^0.16.2",
    "@msgpack/msgpack": "^3.0.0-beta2",
    "@tauri-apps/api": "^2.0.0-alpha.4",
    "@tauri-apps/plugin-shell": "^2.0.0-alpha.0",
    "@tauri-apps/plugin-window": "^2.0.0-alpha.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0-alpha.9",
    "internal-ip": "^7.0.0",
    "typescript": "^5.0.2",
    "vite": "^4.4.4"
  }
}

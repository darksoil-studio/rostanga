{
  "name": "rostanga-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": ["tauri-plugin-holochain/ui"],
  "scripts": {
    "start": "npm run tauri dev -- --release --no-watch --no-dev-server-wait",
    "start:android": "npm run tauri android dev -- --no-dev-server-wait --release --no-watch",
    "android:emulator": "emulator -avd Pixel -gpu off",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "prepare": "curl -L --output gather.webhapp https://github.com/darksoil-studio/gather/releases/download/v0.0.4/gather.webhapp"
  },
  "dependencies": {
    "@holochain/client": "^0.16.2",
    "@msgpack/msgpack": "^3.0.0-beta2",
    "@tauri-apps/api": "^2.0.0-alpha.12"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0-alpha.18",
    "concurrently": "^8.2.1",
    "internal-ip": "^7.0.0",
    "typescript": "^5.0.2",
    "vite": "^4.4.4"
  }
}
